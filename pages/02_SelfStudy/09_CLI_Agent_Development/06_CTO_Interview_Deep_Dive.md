# 面接対策用：技術深掘りと「AIとの境界線」の言語化

本ドキュメントは、最終面接の質疑応答（CTO面談）に向けた最終確認資料である。
自分が実装したものに対し、プロのエンジニアから「なぜそう書いたのか？」と問われた際、知ったかぶりをせず**「自分の判断（設計意図）」と「AIに頼った部分（高度な実装・知見）」の境界線**を正直かつ論理的に説明するための防衛策として設計している。

各技術要素について、以下の3層構造で整理する。

1. **概要:** 何のために存在し、何を実現しているか。
2. **頻出する基礎:** エンジニアが共通認識として持つ、実務上の概念。
3. **理解の境界線（深掘り・AI依存領域）:** 自力では難しく、AIを利用（または学習）した裏側のメカニズム。

---

## 1. Zod（バリデーションと入力の前処理）

### 1-1. 概要

入力されたデータ（AIの出力やユーザーの引数）が正しいフォーマットかを検査し、不適切なものを弾くためのバリデーションライブラリ。

### 1-2. 頻出する基礎

TypeScriptの「型定義（Interfaceなど）」は、コードを書いている最中のエディタ上ではエラーを出してくれるが、プログラム実行時には**全て消滅（Type Erasure）**してしまう。そのため、外部（APIやユーザー）から入ってくる生のデータに対しては、TypeScript自体は何の防御力も持たない。実務では必ずこういった「実行時の検証ライブラリ（Zod）」を入口に配置する。

### 1-3. 理解の境界線（深掘り）

* **自分の判断:** LLM（AI）の出力は『ぎっとはぶ』のように揺らぐ可能性がある。これをそのままシステムに通せば確実にバグになると考え、「入力をふるいにかけて矯正する仕組み」が必要だと判断した。
* **AIに頼った部分:** 具体的にどう実装するかAIに相談したところ、**Zodの `preprocess / transform` を使ったパイプライン処理**を提案された。これにより「ただ型をチェックする」だけでなく「動的にデータを正しい文字列（GitHub）に矯正してから後続へ流す」という高度な前処理が可能になった。この具体的なメソッドと利用法は自分ゼロからは書けず、AIからの学習である。

---

## 2. マスキング層（破壊的サニタイズによるセキュリティ）

### 2-1. 概要

正規表現（Regex）等を用いて、データの中から「IPアドレス」や「アクセストークン」の文字列パターンを検知し、`[MASKED]` といった無害な文字列に置換（サニタイズ）する技術。

### 2-2. 頻出する基礎

外部APIから取得したRawデータを「そのまま別の場所（ログや外部API）」に送信すると重大なセキュリティインシデントに直結する。そのため、プログラムの中継地点に物理的な「関所」を設けて防御的プログラミングを行うのは、バックエンド実務における基本中の基本である。

### 2-3. 理解の境界線（深掘り）

* **自分の判断:** 内部APIを想定した場合、そこからLLMへデータを渡すと「LLMのコンテキストに機密が載り、回答に漏洩する（ハルシネーションリスク）」ことに気づいた。そのため、API通信処理とAgent（AI側）が直接対話できないよう、間に必ずマスキング処理を挟む「関所」のアーキテクチャを決定した。
* **AIに頼った部分:** 「IPアドレス」や「JWTトークン」「UUID」などのセキュリティ上致命的なパターンを正確に検知・破壊する「堅牢な正規表現アルゴリズム」をゼロから書くのは極めて困難（かつバグが許されない領域）であるため、AIに具体的な置換コードを生成させた。また、AIに「他に隠すべき類似パターンはあるか」と相談し、知識を補強した。

---

## 3. Function Calling ループとフェイルセーフ

### 3-1. 概要

AIに最初から「使える機能（API関数）のリスト」を教え、情報が足りないときにはAI自身が「その関数を使ってデータを取ってきて」と要求してくる仕組み。今回はこれを `while` ループで囲んでいる。

### 3-2. 頻出する基礎

プログラムにおける繰り返し処理（`while`）と、それに伴う無限ループの危険性。実務では、外部システムと連携するループには必ず「最大試行回数」や「タイムアウト」等の強制脱出（フェイルセーフ）条件を設ける。

### 3-3. 理解の境界線（深掘り）

* **自分の判断:** エージェントの性質上、目的に到達するまで自律的にAPIを叩き続けることは理解できたが、そのままでは外部APIがダウンしていた際に「回答できない」とAIがパニックを起こし、無限にAPIを叩き続けて無料枠やリソースを枯渇させる危険性に気づいた。そのため、自律ループに対し強制終了させる条件を入れるべきだと判断した。
* **AIに頼った部分:** この「推論 → 行動 → 観察」の仕組みがLLM界隈で **ReActパターン** と呼ばれるステートマシン（状態遷移）であるという学術的背景や、フェイルセーフという名称（Max-Loop Breaker）、および実際にAIからのJSONネストを安全に取り出してループ制御する複雑な実装は、AIと壁打ちして初めて知ったものであり、一から自力で書いたものではない。

---

## 4. Bun ランタイムによるコンパイル (`bun build --compile`)

### 4-1. 概要

自作したTypeScriptのコードを、実行環境が全くない真っ新なPCでもダブルクリック一発で動く「単一の実行可能バイナリ（exe等）」にカプセル化して書き出す技術。

### 4-2. 頻出する基礎

通常、Node.js や Python 等で作ったツールを他人に渡して使ってもらうには「まず実行エンジン（Node.jsなど）をインストールしてください、次にパッケージをインストールしてください」という極めて手間の掛かる環境構築（依存関係の解決）が必要になる。

### 4-3. 理解の境界線（深掘り）

* **自分の判断:** 深夜のインシデント（焦り・疲労状態）での利用を想定した場合、「マニュアルを読んで環境構築する」などの手間は一切排除されるべきだと考え、Zero-setup（無設定）で動く配布形態を強く志向した。
* **AIに頼った部分:** この要件を満たすためにAIに相談した結果、Bunの最新機能である `--compile` を教わった。なぜこれが可能かというと、Bunは自分の実行エンジンである「JavaScriptCore」とソースコードを、C言語レベルで単一ファイルに融合（スタティックリンク）しているからだ、という底层エンジンの仕組みも面接に備えてAIから教わり、キャッチアップした。

---

> 💡 **CTO面接時の無敵の回答スタンス（一言まとめ）**
> 「これらの技術のすべてをゼロから自力で書けたわけではありません。しかし、**『実運用に入れた際のビジネス上のリスク（漏洩・無限ループ・タイポ）』を自分の頭で予測し、AIをメンターとして使い倒して、実務レベルのコードに落とし込みました。**これが私の『AI時代のキャッチアップ力』の証明です。」
